user story - as a user i want to select a deck so i can view and review the cards in that deck.

CI setup - CI is enforced through branch protection rules to prevent integration of failing builds into the main branch.

DTO and service layer implementation

front end slice - 